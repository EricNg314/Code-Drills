USE Mailer_DB;

-- Show all data if Person table's PersonId has a corresponding PersonId from Address table.
SELECT * FROM Person
INNER JOIN Address ON Person.PersonId = Address.PersonId;


SELECT * FROM Person
INNER JOIN Address ON Person.PersonId = Address.PersonId;

SELECT * FROM Person
INNER JOIN Address ON Person.PersonId = Address.PersonId;